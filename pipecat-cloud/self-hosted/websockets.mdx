---
title: "WebSockets in Self-Hosted Regions"
sidebarTitle: "WebSockets & telephony"
description: "Expose a self-hosted region's WebSocket endpoint for telephony and client sessions: Ingress or Gateway API, the public endpoint, token auth."
---

import {
  productName,
  productNameSingular,
  ProductName,
  hostedName,
  cliMinVersion,
  packageMinVersion,
  packageRef,
  registryHost,
} from "/snippets/self-hosted-vars.mdx";

WebSocket sessions connect directly to your region — media never passes through Pipecat Cloud. To use the WebSocket transport or telephony providers with a {productNameSingular}, you expose the region's WebSocket endpoint and register its public URL.

## How sessions reach your region

{/* /start returns wsUrl under the registered endpoint + token */}

## Exposing the endpoint

{/* mode ingress (ALB example, idle timeout) / httpRoute / none */}

## Registering the public endpoint

{/* regions register --ws-public-endpoint; changing it later */}

## Token authentication

{/* per-region signing; websocket_auth token; provider paths require none */}

## Telephony providers

{/* /v1/public/.../start pattern vs provider paths; serviceHost same as cloud */}
