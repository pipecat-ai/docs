---
title: "Self-Hosted Region Requirements"
sidebarTitle: "Requirements"
description: "Cluster, platform, network egress, and node requirements for a Pipecat Cloud self-hosted region, with sizing guidance for platform components."
---

import {
  productName,
  productNameSingular,
  ProductName,
  hostedName,
  cliMinVersion,
  packageMinVersion,
  packageRef,
  registryHost,
} from "/snippets/self-hosted-vars.mdx";

Before registering a {productNameSingular}, make sure your cluster and network meet the requirements below.

## Kubernetes cluster

{/* version floor, cgroup v2, node architectures, PSA note for the workloads namespace */}

## Platform prerequisites

{/* cert-manager + trust-manager; Helm 3.8+ for OCI; kubectl */}

## Network

{/* outbound only: api, registry, region endpoint :8443; inbound only your own WS ingress */}

## Sizing the platform components

{/* platform component requests; the undersized-node lesson; bots pool separate */}

## Tools on your workstation

{/* pipecat CLI >= cliMinVersion, helm, kubectl */}
