---
title: "Operating a Self-Hosted Region"
sidebarTitle: "Operations"
description: "Day-two operations for a self-hosted region: package upgrades, certificate lifecycle, high availability, rebuilding a cluster, and retiring a region."
---

import {
  productName,
  productNameSingular,
  ProductName,
  hostedName,
  cliMinVersion,
  packageMinVersion,
  packageRef,
  registryHost,
} from "/snippets/self-hosted-vars.mdx";

Once a {productNameSingular} is active, most of its lifecycle is hands-free. This page covers the operations you will perform and the ones that happen automatically.

## Upgrading the region package

{/* --version only; never image refs; --reset-then-reuse-values */}

## Certificate lifecycle

{/* automatic; expiry visible in regions show; expired = re-enroll */}

## High availability

{/* agent replicas 2, bot-starter 2, PDBs */}

## Rebuilding a cluster

{/* same key, new token, recreate referenced secrets */}

## Retiring a region

{/* delete agents first; regions delete; refused while agents/sessions exist */}

## GitOps

{/* workloadsNamespace.create false; foreign label */}
