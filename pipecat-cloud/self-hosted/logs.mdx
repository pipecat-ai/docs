---
title: "Self-Hosted Region Logs"
sidebarTitle: "Logs & observability"
description: "Agent logs and session metrics in self-hosted regions: enabling the optional log store, base image requirements, cgroup v2, and platform metrics."
---

import {
  productName,
  productNameSingular,
  ProductName,
  hostedName,
  cliMinVersion,
  packageMinVersion,
  packageRef,
  registryHost,
} from "/snippets/self-hosted-vars.mdx";

Agent logs in a {productNameSingular} are stored in your region and are never sent to Pipecat Cloud for storage. The log store is optional and off by default; when you enable it, `pipecat cloud agent logs`, the dashboard, and the API query it on demand.

## Enabling the log store

{/* logging.enabled + external store (bundled = dev only); schema bootstrap */}

## What is captured

{/* base image >= 0.1.23 for session-stamped runner lines; crash tail */}

## Session metrics

{/* cgroup v2 requirement and degradation */}

## Platform metrics

{/* /metrics on region agent, ServiceMonitor optional */}
