---
title: "What Pipecat Cloud Can See"
description: "Exactly what data the Pipecat Cloud control plane reads from a self-hosted region, what it never reads, and how to turn each channel off."
---

import {
  productName,
  productNameSingular,
  ProductName,
  hostedName,
  cliMinVersion,
  packageMinVersion,
  packageRef,
  registryHost,
} from "/snippets/self-hosted-vars.mdx";

Running a {productNameSingular} means Pipecat Cloud manages your region without holding your data. This page lists exactly what the control plane can read, what it structurally cannot, and how to turn each channel off.

## Always: session records and metrics

{/* telemetry events */}

## By default: platform diagnostics

{/* support bundle contents verbatim from the two Roles; how to disable */}

## Only when you enable it: agent logs

{/* log query lane; off by default; never pulled by Daily */}

## Never

{/* Secret data, env values, ConfigMap bodies, media */}
