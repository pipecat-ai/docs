---
title: "Secrets in Self-Hosted Regions"
sidebarTitle: "Secrets"
description: "Reference Kubernetes Secrets in your cluster as Pipecat Cloud secret sets: env and image-pull secrets, readiness states, and what is recorded."
---

import {
  productName,
  productNameSingular,
  ProductName,
  hostedName,
  cliMinVersion,
  packageMinVersion,
  packageRef,
  registryHost,
} from "/snippets/self-hosted-vars.mdx";

In {productName}, secret values never leave your cluster. Instead of uploading values to Pipecat Cloud, you create a Kubernetes Secret in your workloads namespace and **reference** it; the platform records only the secret's name, region, type, and readiness.

## Referencing a secret

{/* kubectl create secret ...; pipecat cloud secrets reference NAME --region; dashboard */}

## Image pull secrets

{/* docker-registry type auto-detected → imagePullSecret */}

## Readiness

{/* pending/ready/failed/unknown; deleted-underneath; deploy gate 409 */}

## Managing values

{/* rotate with kubectl; redeploy to pick up; managed sets are rejected for self-hosted regions */}

## After rebuilding a cluster

{/* control plane still reports ready; recreate + reference */}
