---
title: "Agent Sizing and Architecture"
description: "Size agents in self-hosted regions with explicit CPU and memory or organization agent profiles, and target amd64 or arm64 nodes per deployment."
---

import {
  productName,
  productNameSingular,
  ProductName,
  hostedName,
  cliMinVersion,
  packageMinVersion,
  packageRef,
  registryHost,
} from "/snippets/self-hosted-vars.mdx";

{hostedName} size agents with a fixed set of agent profiles. In {productName} you control the hardware, so you can size each deployment explicitly or define your own profiles.

## Explicit resources

{/* deploy --resources cpu=,memory=; [resources] toml; dashboard; mutually exclusive with profile */}

## Organization agent profiles

{/* agent profiles list/create/update/enable/disable; snapshot at deploy semantics */}

## Architecture

{/* region declares supported/default; deploy --architecture; validation error */}

## Scaling limits

{/* org replica ceiling does not apply; capacity notes */}
