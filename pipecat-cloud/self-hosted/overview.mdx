---
title: "Self-Hosted Regions"
sidebarTitle: "Overview"
description: "Run Pipecat Cloud agents in your own Kubernetes cluster as a self-hosted region: what stays managed, what you operate, what leaves your cluster."
---

import {
  productName,
  productNameSingular,
  ProductName,
  hostedName,
  cliMinVersion,
  packageMinVersion,
  packageRef,
  registryHost,
} from "/snippets/self-hosted-vars.mdx";

A {productNameSingular} is a Pipecat Cloud region that runs in a Kubernetes cluster you operate. Pipecat Cloud remains the control plane: you deploy, start sessions, and read logs and session records exactly as you do for {hostedName}, while your agents execute on your infrastructure.

## What stays the same

{/* deploy / start / sessions / logs / dashboard / CLI parity */}

## What changes

{/* secrets are referenced, you size explicitly, you own ingress + stores, architecture is yours */}

## What leaves your cluster

{/* session metadata + metrics events; log lines only when you enable the lane and query; never media, never secret values */}

## How to get started

{/* entitlement: enterprise feature, contact Daily; then Requirements → Set up */}
