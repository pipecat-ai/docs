---
title: "Set Up a Self-Hosted Region"
sidebarTitle: "Set up a region"
description: "Register a self-hosted region, mint an enrollment token and registry key, install the region package with Helm, and verify the region is active."
---

import {
  productName,
  productNameSingular,
  ProductName,
  hostedName,
  cliMinVersion,
  packageMinVersion,
  packageRef,
  registryHost,
} from "/snippets/self-hosted-vars.mdx";

This guide takes a cluster that meets the [requirements](/pipecat-cloud/self-hosted/requirements) to an active {productNameSingular} your organization can deploy agents to.

<Steps>
  <Step title="Mint a registry key">
    {/* organizations registry-keys mint; helm registry login */}
  </Step>
  <Step title="Register the region">
    {/* regions register: key, display name, workloads namespace, architectures, WS endpoint; dashboard equivalent */}
  </Step>
  <Step title="Mint an enrollment token">
    {/* regions enroll-token; kubectl create secret; one-time, 1h */}
  </Step>
  <Step title="Install the region package">
    {/* helm upgrade --install ... --set global.regionKey ... --wait */}
  </Step>
  <Step title="Verify">
    {/* regions show → active; pods Running; enrollment Job Completed */}
  </Step>
</Steps>

## Next steps

{/* secrets, deploy an agent, WebSockets */}
