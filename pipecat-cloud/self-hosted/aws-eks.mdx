---
title: "Self-Hosted Region on AWS EKS"
sidebarTitle: "AWS EKS reference"
description: "A reference architecture for a self-hosted region on Amazon EKS: VPC, node pools, managed broker and log store, WebSocket front door, monitoring."
---

import {
  productName,
  productNameSingular,
  ProductName,
  hostedName,
  cliMinVersion,
  packageMinVersion,
  packageRef,
  registryHost,
} from "/snippets/self-hosted-vars.mdx";

This reference architecture describes a production-shaped {productNameSingular} on Amazon EKS. It is one proven layout, not a requirement — any conforming Kubernetes cluster works.

## Layout

{/* VPC, system node group, bots pool (Karpenter), managed Valkey + OpenSearch */}

## WebSocket front door

{/* ALB + ACM + external-dns; idle timeout */}

## Values

{/* what the values file carries */}

## Monitoring

{/* optional kube-prometheus-stack; what to watch */}

## Cost and sizing presets

{/* dev/small/load/production shapes */}
