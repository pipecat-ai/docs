---
title: "Self-Hosted Region Diagnostics"
sidebarTitle: "Diagnostics & support"
description: "Diagnose a self-hosted region with the doctor, region status and event feed, and the support bundle, and what to collect before contacting support."
---

import {
  productName,
  productNameSingular,
  ProductName,
  hostedName,
  cliMinVersion,
  packageMinVersion,
  packageRef,
  registryHost,
} from "/snippets/self-hosted-vars.mdx";

A {productNameSingular} ships with its own diagnostics, and the control plane keeps a record of what happened to it. Start here when something is off.

## Region status

{/* regions show: status, connected agents, versions, cert expiry; dashboard */}

## Region events

{/* event feed: enrolled, renewal.signed/rejected/overdue, revoked */}

## The doctor

{/* two forms from helm get notes; what it checks; nothing leaves the cluster */}

## Support bundle

{/* what it is, on by default, audited, how to disable */}

## Common failures

{/* 403 on register; enroll Job failed; ImageResolutionFailed vs CreateContainerConfigError; --wait timeout; architecture not supported; wsUrl stale */}

## Contacting support

{/* what to collect: region key, request id, doctor output; logs posture */}
