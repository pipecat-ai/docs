export const SpareInstancesNote = () => (
  <Note>
    If you keep warm instances (<code>min-agents</code> 1 or higher), Pipecat
    Cloud keeps a few spare instances above the limits you set, so the instance
    count you see (<code>readyReplicas</code> in the API, or the count in the
    Pipecat Cloud Dashboard) is normally higher than <code>max-agents</code>,
    even at zero traffic. Spares do not raise your session limit or change your
    bill. See{" "}
    <a href="/pipecat-cloud/fundamentals/scaling#instance-count-vs-configured-agents">
      Instance count vs. configured agents
    </a>
    .
  </Note>
);
