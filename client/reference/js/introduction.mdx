---
title: 'SDK Introduction'
description: 'Build web applications with Pipecat’s JavaScript client library'
---

The Pipecat JavaScript SDK provides a lightweight client implementation that handles:

- Device and media stream management
- Managing bot configuration
- Sending actions to the bot
- Handling bot messages and responses
- Managing session state and errors

## Installation

Install the SDK and a transport implementation (e.g. Daily for WebRTC):

```bash
npm install @pipecat-ai/pipecat-js
npm install @pipecat-ai/realtime-ai-daily
```

## Quick Example

Here's a simple example using Daily as the transport layer:

```javascript
import { PipecatClient } from '@pipecat-ai/pipecat-js';
import { DailyTransport } from '@pipecat-ai/pipecat-daily';

// Handle incoming audio from the bot
function handleBotAudio(track, participant) {
  if (participant.local || track.kind !== 'audio') return;

  const audioElement = document.createElement('audio');
  audioElement.srcObject = new MediaStream([track]);
  document.body.appendChild(audioElement);
  audioElement.play();
}

// Create and configure the client
const client = new PipecatClient({
  params: {
    baseUrl: process.env.PIPECAT_API_URL || '/api',
  },
  transport: new DailyTransport(),
  enableMic: true,
  callbacks: {
    onTrackStart: handleBotAudio,
  },
});

// Connect to your bot
client.connect();
```

## Interaction Patterns

The SDK supports two main patterns for interacting with your bot:

### Real-time Connections

For voice and video chatbots, establish an ongoing connection using `connect()`. This enables:

- Live voice conversations
- Real-time video processing
- Continuous interactions

### Single-turn Actions

For simpler use cases, use `action()` to send individual requests:

- Text-to-text chat
- One-off image processing
- Batch operations

## SDK Structure

<CardGroup cols={2}>
  <Card title="Client Core" icon="cube">
    Constructor and methods for basic client operations
  </Card>
  <Card title="Transport" icon="network-wired">
    Interfaces for WebRTC and WebSocket implementations
  </Card>
  <Card title="Services" icon="puzzle-piece">
    Integrations with AI services and APIs
  </Card>
  <Card title="Helpers" icon="wand-magic-sparkles">
    Utility functions for common operations
  </Card>
</CardGroup>

## Next Steps

- Learn about the [Client Constructor](/client/reference/js/client-constructor) options
- Explore available [Client Methods](/client/reference/js/client-methods)
- Check the [API Reference](/client/reference/js/api-reference) for detailed documentation

<Note>
  For production deployments, we recommend setting up authentication endpoints
  rather than exposing API keys directly in client code.
</Note>
