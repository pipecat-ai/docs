---
title: 'Media Service'
description: 'Base implementations for image and vision services'
---

## ImageGenService

<ResponseField name="ImageGenService" type="base class">
  Base class for [Image Generation
  services](/api-reference/services/supported-services#image-generation).
</ResponseField>

### Methods

<ResponseField name="run_image_gen" type="async method">
  Core image generation method to be implemented by services.

```python
@abstractmethod
async def run_image_gen(self, prompt: str) -> AsyncGenerator[Frame, None]
```

</ResponseField>

## VisionService

<ResponseField name="VisionService" type="base class">
  Base class for [Vision
  services](/api-reference/services/supported-services#vision).
</ResponseField>

### Methods

<ResponseField name="run_vision" type="async method">
  Core vision processing method to be implemented by services.

```python
@abstractmethod
async def run_vision(
  self,
  frame: VisionImageRawFrame
) -> AsyncGenerator[Frame, None]
```

</ResponseField>
